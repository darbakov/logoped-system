# Database (SQLite)
DATABASE_URL="file:/app/data/logopro.db"

# Auth.js secret (generate with: openssl rand -hex 32)
AUTH_SECRET="your-secret-here"

# URL where your app is hosted
NEXTAUTH_URL="https://your-domain.com"
